/* DEPRECATED: legacy root app (kept for reference).
   Use the React app under `src/` (entry: `src/main.tsx`, router: `src/app/Router.tsx`).
   This file is intentionally left as a stub to avoid accidental usage.
*/

export default null as unknown as () => JSX.Element;

