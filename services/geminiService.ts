/* DEPRECATED: legacy Gemini service at repo root.
   Use `src/shared/services/geminiService.ts` instead (exports `sendCopilotMessage`).
*/

export const sendCopilotMessage = async (): Promise<string> => {
  return 'Deprecated: use src/shared/services/geminiService.ts';
};
